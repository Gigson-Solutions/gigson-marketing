# Plan de implementación — Auditoría GEO

**Fecha:** 2026-09-21
**Companion de:** [docs/AUDIT_GEO_2026-09-21.md](AUDIT_GEO_2026-09-21.md)
**Rama base:** `staging`

## Estado de ejecución (actualizado 2026-09-21)

Los seis bloques están implementados en la rama `claude/gigsonsolutions-geo-audit-beg4fi`,
rebasada sobre `staging`. La PR va **contra `staging`**.

| Bloque | Estado | Commit |
|---|---|---|
| 1 · Entidad (`seo/12`) | Hecho | `fix(seo): repair the Organization logo and email…` |
| 2 · Home + FAQ (`seo/13`) | Hecho | `feat(seo): give the home page a definition…` |
| 3 · Precios (`seo/14`) | Hecho | `feat(seo): publish the pricing ranges as a page` |
| 4 · Casos (`seo/15`) | Código hecho; **falta seed** | `feat(seo): give every case study a URL of its own` |
| 5 · llms (`seo/16`) | Hecho | `feat(seo): describe every link in llms.txt…` |
| 6 · Contenido | Borradores listos; **falta publicar** | `content: get the two unpublished drafts ready…` |

Verificación en cada bloque: `npx eslint .` con **0 errores** y `npm run build` correcto.

### Pasos manuales pendientes (no se pueden hacer desde el repo)

1. **Migración de BD.** `migrations/20260921_120000_add_cases.ts` se aplica sola en el deploy
   (`prodMigrations`). No se ha ejecutado contra una base real aquí: el log de build de la preview
   de Vercel es la verificación.
2. **Seed de los casos.** Hasta que se ejecute, `/casos` sigue mostrando el contenido de
   `messages/*.json` y no hay páginas de detalle. Nada se rompe mientras tanto.
   ```bash
   PAYLOAD_MIGRATING=true node --env-file-if-exists=.env.local \
     scripts/run-payload-script.mjs scripts/seed-cases.mts            # dry run
   PAYLOAD_MIGRATING=true CASES_CONFIRM=1 node --env-file-if-exists=.env.local \
     scripts/run-payload-script.mjs scripts/seed-cases.mts
   ```
3. **Publicar los dos borradores** copiando el cuerpo al editor Lexical de `/admin`, pasando la
   lista "Lo esencial" al bloque `keyTakeaways` y asignando `authorProfile`, `locale` y `category`.
4. **URLs reales para `sameAs`** (directorio de Claude Partners, Crunchbase, GitHub, X, Google
   Business Profile). Sin ellas el `sameAs` sigue teniendo un solo enlace; no se inventan.
5. **Slug del fundador** en la colección `Authors`, para conectar `Organization.founder` al nodo
   `Person` que ya existe. Un slug adivinado emitiría una referencia colgante.
6. **Sprint 4 off-page**, que sigue íntegro y es la mitad del trabajo GEO.

### Decisión pendiente

El aviso legal identifica la entidad como **Awesomely, Sociedad Limitada** (CIF B22482137).
Añadir `legalName` y `vatID` al `Organization` reforzaría la desambiguación, pero si la separación
marca/sociedad es deliberada podría confundir. No se ha añadido.

### Hallazgo que sigue abierto

48 de las 66 URLs del sitemap (las rutas estáticas) no llevan `lastmod`. `app/sitemap.ts` lo
documenta como decisión deliberada — evitar una fecha de build fabricada — así que no se ha
tocado. Resolverlo bien requiere la fecha del último commit que tocó cada namespace de
traducciones.

---

## Contexto

La auditoría (`docs/AUDIT_GEO_2026-09-21.md`, commit `e63c624`) midió **producción**, que sirve
`main`. Pero el trabajo vive en `staging`, **39 commits por delante**, donde ya se ejecutó un
workstream SEO completo (PRs `seo/01`…`seo/11`). Buena parte de la auditoría **ya está resuelta ahí
y solo pendiente de release**. Este plan cubre únicamente lo que sigue sin hacer en `staging`,
continuando esa misma numeración.

Decisiones tomadas por el usuario:
- **Alcance:** todo el trabajo restante.
- **Precios:** publicar las horquillas completas (8.000–15.000 € / 20.000–50.000 €).
- **Casos:** nueva colección Payload (no seguir en `messages/*.json`).

### Rama base — crítico

El trabajo **debe salir de `staging`**, no de `main`:
- `README.md` es explícito: *"El trabajo normal sale de `staging` y vuelve a `staging`."*
- `.github/workflows/pr-source-guard.yml` bloquea toda PR a `main` que no venga de `staging` o `hotfix/*`.
- **Todos** los ficheros a tocar difieren entre `main` y `staging` (`lib/schema.ts`, `app/sitemap.ts`,
  `messages/*.json`, `app/[locale]/(site)/blog/[slug]/page.tsx`).

Primer paso: rebasar la rama actual sobre `origin/staging`, conservando el commit de la auditoría.

```bash
git fetch origin staging
git rebase origin/staging   # sobre claude/gigsonsolutions-geo-audit-beg4fi
```

### Lo que `staging` ya cubre — NO rehacer

| Hallazgo de la auditoría | Estado en `staging` |
|---|---|
| §3.3 `dateModified` en `Article` | Hecho — `blog/[slug]/page.tsx:143,176` |
| §3.3 `lastmod` en sitemap | Hecho — `app/sitemap.ts:89,98,99` + `getPostIndex()` en `lib/posts.ts:173` |
| §6.1 `llms.txt` sin blog | Hecho — ahora ruta dinámica `app/llms.txt/route.ts` (`public/llms.txt` eliminado) |
| §5.1 Sin entidad `Person` / E-E-A-T | Hecho — colección `Authors`, `buildPersonSchema()`, rutas `/blog/autores/[author]` |
| Bloque extraíble para motores | Hecho — `KeyTakeawaysBlock` → `BlogPosting.abstract` |
| Prerrequisito de rutas dinámicas | Hecho — `StaticPathnames` (`i18n/routing.ts:74`) excluye `[slug]` automáticamente |

Consecuencia útil: **añadir `'/cases/[slug]'` a `routing.pathnames` no obliga a tocar ningún fichero
de navegación.** `StaticPathnames` es un mapped type que filtra cualquier pathname con `[`; los cinco
`Exclude<AppPathnames, '/blog/[slug]'>` que existían se consolidaron en `seo/08`.

---

## Bloque 1 — `seo/12-entidad`: correcciones P0 + `Organization` completa

Todo en `lib/schema.ts`. Es el bloque de mayor retorno por esfuerzo.

**1.1 Logo 404.** `/gigson-logo.svg` no existe (verificado: 404 en producción). Aparece **dos veces**
en staging: `organizationMinimal()` (L35) y `buildOrganization()` (L66). Apuntar ambas a
`/img/gigson-solutions-logo.png` (existe, 437×122 px, cumple el mínimo de 112 px de Google).
Extraer a una constante `LOGO_URL` para que no vuelvan a divergir.

**1.2 Email.** `buildOrganization()` L83 declara `hola@gigsonsolutions.com`; el resto del sistema usa
`info@` (`messages/{es,en}.json:587`, `src/lib/gigson.ts:22,113`, `GigsonChatbotWidget.tsx:47,73`).
`hola@` es el único outlier → cambiar a `info@`.

**1.3 Completar la entidad.** Añadir a `buildOrganization()`:

| Propiedad | Valor | Origen del dato |
|---|---|---|
| `address` (`PostalAddress`) | `addressLocality: 'Barcelona'`, `addressCountry: 'ES'` | Barcelona aparece 4× en `messages/es.json` |
| `telephone` | `+34 630 840 225` | `src/lib/gigson.ts` |
| `email` (nivel raíz) | `info@gigsonsolutions.com` | además del `contactPoint` |
| `hasCredential` | `EducationalOccupationalCredential`, `name: 'Certified Anthropic Claude Partner'`, `recognizedBy: { '@type': 'Organization', name: 'Anthropic', url: 'https://www.anthropic.com' }` | **el diferenciador nº 1, hoy solo como texto** |
| `founder` | `{ '@id': authorUrl(<slug>, 'es') + '#person' }` | reutilizar la entidad `Person` que ya crea `buildPersonSchema()` |
| `sameAs` | ampliar más allá de LinkedIn | ver nota abajo |

`founder` debe **referenciar** el `Person` de la colección `Authors` por `@id`, no declarar uno nuevo
— mismo principio que `ORGANIZATION_ID`: un nodo, referenciado en todas partes.

> **Requiere input del usuario:** las URLs reales para `sameAs` (ficha en el directorio de Claude
> Partners, Crunchbase, GitHub, X, Google Business Profile) y confirmar Barcelona + teléfono como
> datos públicos. Si no están disponibles al implementar, se añade lo que haya y se deja el resto
> anotado; **no inventar URLs**, un `sameAs` roto es peor que uno corto.

---

## Bloque 2 — `seo/13-home-faq`: copy citable

**2.1 Párrafo definitorio en la home.** Hallazgo aprovechable: `home.heroSubhead`
(`messages/{es,en}.json:80`) **ya existe en ambos idiomas y no se renderiza en ningún sitio**
(0 usos en `.ts`/`.tsx`). Su contenido es casi exactamente la frase definitoria que pide la auditoría.

- Reescribirla en forma sujeto–verbo–categoría–ubicación, al estilo del `llms.txt` (que sí lo hace
  bien): *"Gigson Solutions es una consultora tecnológica española, Partner Certificado de Anthropic,
  que diseña y despliega agentes de IA basados en Claude para empresas medianas en España."*
- Renderizarla en `src/components/Home/Hero.tsx`, **entre L27 (`<RichText as="h1">`) y L28
  (`<Link href="/contact">`)**, dentro de `<div className="hero-text">`. Usar `<RichText as="p">` si
  lleva `<span>`. Añadir `.hero-lede` a `src/components/Home/Hero.css` (no existe).

**2.2 Cifras citables en home.** La home renderiza 2.153 palabras y **cero cifras**. Introducir en
`home.servicesP` / una sección nueva los datos que ya son públicos en otras páginas: +11 años,
4–8 semanas a producción, Partner Certificado de Anthropic, 5 sectores. Sin inventar nada: todo sale
de `src/lib/gigson.ts`.

**2.3 Limpieza de la FAQ.** `faqsDropdown` es un array plano raíz de 16 entradas (4 cabeceras con
`title` + 12 Q&A). El mismo array alimenta el acordeón y el `FAQPage` JSON-LD
(`app/[locale]/(site)/faqs/page.tsx:45`), así que un solo cambio corrige ambos.

- **Typo:** `faqsDropdown[6].answer` (`messages/es.json:553`) dice `colaboradres` → `colaboradores`.
  Solo en ES; el EN está bien.
- **Quitar el CTA modelado como pregunta:** `faqsDropdown[15]` (*"¿Nos hemos dejado algo por
  contestar?"*) no es una FAQ y ensucia el `FAQPage`.

  ⚠️ **Borrarlo sin más rompe la UI.** `Faqs.tsx:66` usa `isLast={index === faqsData.length - 1}` —
  posicional, no por contenido. Al eliminar `[15]`, el `isLast` recae sobre `[14]`
  (*"¿Cuánto cuesta un agente de IA?"*), que no tiene `cta`, y `FaqsAccordion` pintaría un botón
  morado con etiqueta vacía dentro de esa respuesta. Hay que cambiar la condición a contenido
  (`cta={item.cta}` y renderizar el CTA solo si `item.cta` existe, en `FaqsAccordion.tsx:29-35`) o
  sacar el "¿Hablamos?" a un bloque propio tras el acordeón.
- **Reenfocar hacia consultas de era IA:** 9 de 12 preguntas son de la etapa "software a medida".
  Añadir preguntas que hoy se hacen a los motores, con cifras en la respuesta. Seguir la convención
  ya establecida en las páginas de servicio: `<namespace>.faq.items` con `{question, answer}`
  (14 namespaces la usan; `faqItemsFrom()` en `lib/schema.ts`).

---

## Bloque 3 — `seo/14-precios`: página de precios

Decisión confirmada: publicar las **horquillas completas**.

- Nueva ruta `app/[locale]/(site)/pricing/page.tsx` + entrada en `i18n/routing.ts`:
  `'/pricing': { en: '/pricing', es: '/precios' }`.
- Contenido desde `src/lib/gigson.ts` (ya escrito, solo hay que exponerlo): agente básico
  8.000–15.000 €, proyectos complejos con varias integraciones 20.000–50.000 €, retainer mensual,
  plazos 4–8 / 10–16 semanas, resultados visibles desde la semana 2–3, y qué incluye cada tramo.
- Schema: `Service` con `offers` (`Offer` / `AggregateOffer` con `priceCurrency: 'EUR'`,
  `lowPrice` / `highPrice`) + `FAQPage` propio. Reutilizar `buildServiceSchema()` y `buildFaqSchema()`.
- Añadir a `STATIC_ROUTES` en `app/sitemap.ts`, al `llms.txt` y a la navegación.
- Formato tabla/lista: es lo que un motor extrae mejor.

Nota: `aiAgents.faq` **ya publica** el rango 8.000–15.000 € en `/es/agentes-ia`. Esta página lo
consolida y lo hace direccionable, no rompe una política previa de precios ocultos.

---

## Bloque 4 — `seo/15-casos`: colección Payload + `/casos/[slug]`

El bloque más grande. Hoy los casos viven en `messages/{en,es}.json` bajo `casesDropdown`
(**5 casos**, alineados por índice EN↔ES, **sin slug, sin id, sin imagen, sin fecha**), leídos por
`Cases.tsx:43`, `HomeCases.tsx:29` y `cases/page.tsx:44`.

**4.1 Colección `Cases`.** Calcar `collections/Authors.ts` (mismo patrón, añadida en `seo/09`):
campos `title`, `slug` (unique), `challenge`, `features[]`, `solution`, `tools[]`, `results[]`,
`tags[]`, `need[]`, `locale` ('es'|'en'), `localizedVersion`, `coverImage`, `publishedAt`, `status`.
Registrar en `payload.config.ts:35`.

**4.2 Migración.** El adaptador es Postgres con `prodMigrations`; una colección nueva = tabla nueva.
Escribir `migrations/<fecha>_add_cases.ts` siguiendo `migrations/20260918_130000_add_authors.ts` y
registrarla en `migrations/index.ts`.

**4.3 Migrar los 5 casos** de `casesDropdown` a la colección (script puntual con
`scripts/run-payload-script.mjs`, que ya es el patrón para escribir en Payload). Slugs traducidos por
idioma, como el blog (ES y EN son documentos distintos unidos por `localizedVersion`).

**4.4 Ruta `app/[locale]/(site)/cases/[slug]/page.tsx`.** Calcar `blog/[slug]/page.tsx`:
- `params: Promise<{locale, slug}>` (Next 16 async), `export const revalidate = 3600`.
- `generateStaticParams()` con el producto locale × slugs.
- `generateMetadata()` con canonical vía un helper `caseUrl()` — **`localizedUrl()` no sirve para
  rutas dinámicas**, hace lookup literal en la tabla y devolvería `/es/casos/[slug]`. El blog ya lo
  resuelve así con `postUrl()` (`lib/schema.ts:170`).
- `alternates.languages` solo si existe `localizedVersion` (el `LanguageSelector` lee el
  `<link rel="alternate">` del DOM; si falta, conviene un fallback `pathname.startsWith('/cases/')`
  análogo al del blog).
- Añadir `'/cases/[slug]': { en: '/cases/[slug]', es: '/casos/[slug]' }` a `routing.pathnames`.
  Gracias a `StaticPathnames`, **no hay que tocar Footer, Navbar ni los demás**.
- Reutilizar `src/components/Accordion/Accordion.tsx`, que ya pinta challenge/features/results/
  solution/tools.

**4.5 Enlazar e indexar.** Cada tarjeta del índice enlaza a su detalle; añadir `url` a cada
`ListItem` del `ItemList` en `cases/page.tsx:49-61` (hoy no lo tiene); `getCaseEntries()` en
`app/sitemap.ts` en paralelo a `getBlogEntries()`; schema por caso (`Article` o `CaseStudy`) con
resultados medibles.

**4.6 Middleware.** Si los slugs se traducen (como el blog), `/casos/<slug>` sin prefijo necesita la
misma rama de `localeDetection: false` que ya tiene `UNPREFIXED_BLOG_POST` en `middleware.ts:22-31`.
Si ES y EN comparten slug, no hace falta.

---

## Bloque 5 — `seo/16-llms`: `llms.txt` + `llms-full.txt`

`app/llms.txt/route.ts` ya incluye el blog con excerpt y fecha (`seo/06`). Queda:
- **Descripciones por enlace** en las listas de páginas prioritarias (hoy son enlaces pelados; el
  formato admite `- [Título](url): descripción`, y es justo lo que orienta al modelo).
- Añadir precios y casos a la lista una vez existan los bloques 3 y 4.
- **`app/llms-full.txt/route.ts`** (hoy 404): volcado del corpus de `src/lib/gigson.ts` — precios,
  plazos, proceso de 4 pasos, stack por sector, casos con resultado. Es el mejor material GEO de la
  empresa y hoy no lo ve ningún crawler.
- Línea de última actualización.

---

## Bloque 6 — Contenido: publicar los 2 borradores

`content/blog-drafts/odoo-vs-holded-vs-erp-a-medida.md` y `odoo-ia-casos-reales-automatizacion.md`.

⚠️ **No están listos para publicar tal cual:**
- Tienen **636 y 443 palabras** frente al objetivo de **900–1300** de su propia plantilla
  (`_TEMPLATE.md`). Las cifras de la tabla del README (~950, ~900) están infladas.
- Al comparativo *Odoo vs Holded vs ERP a medida* **le falta la tabla comparativa**, que es
  precisamente el formato por el que ese artículo es valioso para un motor.

Pasos: ampliar ambos hasta el rango objetivo, añadir la tabla al comparativo, añadir un
`KeyTakeawaysBlock` a cada uno (el bloque de `seo/07`, que alimenta `BlogPosting.abstract`), asignar
`authorProfile` de la colección `Authors`, y publicar.

**El flujo es manual**: `content/blog-drafts/README.md` documenta copiar el `.md` al editor Lexical
en `/admin`; no existe importador. `scripts/run-payload-script.mjs` + `upload-blog-covers.mts` son el
patrón si se prefiere scriptarlo. Ojo: `locale`, `category` y `localizedVersion` no están en el
frontmatter y hay que fijarlos a mano; las portadas se registran en `upload-blog-covers.mts` (`SPECS`)
y `lib/blogCovers.ts` (`COVER_BY_SLUG`).

---

## Orden de ejecución

Una PR por bloque hacia `staging`, continuando la numeración `seo/*`:

1. **`seo/12-entidad`** — independiente, el de mayor retorno. Primero.
2. **`seo/13-home-faq`** — independiente, solo copy + dos componentes.
3. **`seo/14-precios`** — nueva ruta.
4. **`seo/15-casos`** — el más grande; incluye migración de BD.
5. **`seo/16-llms`** — **al final**, para poder referenciar precios y casos.
6. **Contenido** — los borradores, en paralelo a cualquiera de los anteriores.

## Verificación

CI (`.github/workflows/ci.yml`) exige `npx eslint .` + `npm run build`. Antes de cada push:

```bash
npx eslint .          # modo verificación, sin --fix (como CI)
npm run build
```

El bloque 4 además necesita Postgres local en el 5546 (ver `README.md`) para probar la migración y
la colección.

Comprobaciones específicas por bloque:

```bash
# Bloque 1 — el logo ya resuelve y el schema es válido
curl -sS -o /dev/null -w "%{http_code}\n" https://<deploy>/img/gigson-solutions-logo.png
curl -sS https://<deploy>/es | grep -o '"@type":"[^"]*"' | sort | uniq -c
# y pasar la home por el validador de resultados enriquecidos de Google

# Bloques 2-4 — contenido visible para un crawler sin JS
curl -sS -A "Mozilla/5.0 (compatible; GPTBot/1.0)" https://<deploy>/es | grep -c "Partner Certificado"
curl -sS -A "Mozilla/5.0 (compatible; GPTBot/1.0)" https://<deploy>/es/precios | head -c 3000
curl -sS https://<deploy>/es/casos/<slug> | grep -o '"@type":"[^"]*"'

# Bloque 5
curl -sS https://<deploy>/llms.txt
curl -sS -o /dev/null -w "%{http_code}\n" https://<deploy>/llms-full.txt

# Sitemap: que las nuevas URLs entran y llevan lastmod
curl -sS https://<deploy>/sitemap.xml | grep -c "<loc>"
curl -sS https://<deploy>/sitemap.xml | grep -c "lastmod"
```

Regresión a vigilar en el bloque 2: tras quitar `faqsDropdown[15]`, abrir `/es/preguntas-frecuentes`
y confirmar que **no** aparece un botón morado vacío al final de la respuesta de precios, y que el
`FAQPage` emite 11 `Question` en vez de 12.

## Fuera de alcance (no es código)

El Sprint 4 de la auditoría es off-repo y sigue pendiente: reclamar la ficha en el directorio de
Claude Partners, nota propia sobre la certificación, ítem en Wikidata, perfiles en Clutch/G2. Es la
mitad del trabajo GEO — los motores ponderan mucho lo que dicen terceros — pero no se resuelve desde
este repositorio. Alimenta directamente el `sameAs` del bloque 1.

Housekeeping menor: `docs/seo-phase-0-baseline.md` describe la arquitectura Vite/S3/CloudFront
anterior a Next.js y ya no aplica; conviene marcarlo como histórico.
